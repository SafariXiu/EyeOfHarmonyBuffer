# Talos 地形管线 V2 · 设计文档

> 本文件是 V2 地形重构的唯一设计记录，随讨论/实施实时更新。
> 目标：把海陆分布升级到拟真地球级，整条地形管线成为**单一骨架派生的单向管线**，
> 全程保持极低性能开销（运行期单点采样 ≈ 0 增量）。
>
> 当前分支：ArknightsProject-terrain（地形专用分支）。

---

## 一、决策记录（已拍板）

| # | 决策 | 内容 |
|---|---|---|
| D1 | 总目标 | 拟真地球级海陆分布；单源派生单向管线；极低性能 |
| D2 | 单向派生纪律 | 任何层不得反向依赖下游（见依赖图） |
| D3 | L0 独立成层 | 洋流/风带是行星边界条件；独立包 chunk/circulation_layer/ |
| D4 | 洋流 = 环面+显式漩涡 | 双周期世界（X=400k 周期，Z=200k 纬度循环）→ 洋流天然闭合；显式放 4~6 个可调试漩涡中心 + 纬向风驱基底 |
| D5 | 山脉现状 | 板块挤压带→TectonicStyleLayer→山带洪泛→DLA 内部结构 |
| D6 | 水系升级 | 替换模板链，新增 WatershedBuilder；下游 RiverNetwork 100% 复用 |
| D7 | 性能预算 | 低频场 + 每大陆一次性生成 + tile 缓存；运行期 ≈ 0 |
| D8 | 纬度循环 | 200k 周期；热带 0/±200k；寒带 ±100k（冷接冷）；阈值 ×2 |
| D9 | 旧世界 | 不在乎旧档/旧种子；测试直接开新档 |
| D10 | 大陆密度 | 目标 **30~35%** 陆地占比 |
| D11 | 山脉约束 | 山带**只在陆地(isLand)生成**；海洋内后续另做海脊/海沟；大陆内部做裂谷 |
| D12 | 河流形态 | 每大陆**至少一条贯穿型巨河**（跨越大半个大陆），最好多条；完整水系：主干+支流+各类湖泊，有头有尾。**宽度渐变**：源头窄（10~20 格半径）→ 入海口宽（200~400 格），越往下越宽 |
| D13 | 群系重做 | 群系层**完全重做逻辑**，以 L0 气候为标准；引入海洋群系（暖洋流/寒洋流等）；排期 S5 |
| D14 | 洋流环面几何 | X 方向也接成周期（**400k**），世界 = 400k(X) × 200k(Z) 双周期环面（洋流模块用） |
| D15 | **洋流推迟到 S4** | 依据权威海洋学因果链：风场是发动机、大陆是模具。没有大陆就没有环流圈/暖寒流。洋流整体推迟到 **S4（大陆之后）**，不作为独立层先行 |
| D16 | 洋流 = 风 × 大陆 | 洋流 = 风驱纬向流 + 大陆折射（沿岸修正东岸暖/西岸冷）。环流旋向 = 科里奥利（北顺南逆）。冷暖随流向走（暖流红/寒流蓝） |
| D17 | 海绝对连通 | **海洋必须绝对连通**：大陆内部不会有海。地中海这类"有链接的海"= 大洋海湾算海；里海/死海类"无链接"= 湖，归水系系统。低于海平面的内陆洼地仍是**陆地**，是否成湖/干盆地由水系决定（现有"非陆即海"水场规则需改） |
| D18 | 岛链 | **显式生成岛链**：在汇聚边界/大陆边缘撒群岛（岛弧风格），L1 处理 |
| D19 | 气压显式输出 | L0 **显式输出气压带/沙漠带**信号（副热带高压=干燥带），供 L1/L5 消费 |
| D20 | 水系独立 | 大陆**内部的水系完全由水系系统决定**（海陆层不掺和内部水体） |
| D21 | 气团定稿（2026-09） | 气团 = AirMassField 简单分类版（isLand × bandD → cP/mP/mT/cT + 温度 + 湿度），依附海陆、确定性、O(1)。**废弃**“气团种子 G=250k/体量半径 30k → 地转风”方案（旧 AirMassSystem 已删除，git 历史可找回）；该方案的风场职责改由 D22 承担 |
| D22 | 风场定稿（2026-09） | 风 = 三圈环流纬向基底（权重 1.0，方向稳定主导）+ 15° 斜向（信风朝赤道/西风朝极地/极地东风朝副极地）+ 半固定气压系统弱扰动（切向 0.18 / 径向 0.08）。系统影响距离按环面**最小镜像回卷**（X=400k / Z=200k），保证接缝两侧与南赤道（ITCZ 经回卷覆盖 z≈200k 一侧）连续 |
| D23 | 海岸距离语义（2026-09） | NoiseContinentGrid 只出**块级**有符号海岸距离 coastDistBlocks（残差/梯度中央差分，±200k 截断）；旧 signedDist 把噪声高度残差当 block 距离用，近岸判定全图恒真，已废弃。等值线附近做鞍部抬升（×2 拉伸合并近邻小块） |
| D24 | 统一气候门面（2026-09） | GlobalClimate.sample（circulation_layer）= 对外唯一 API，一次返回 ClimateSample（L1 海陆/海岸 + L0 环流 + L2 气团 + L4 洋流）；/talosmap 与未来 L5 群系消费方一律走它，禁止散调各层静态入口 |
| D25 | S6 洋流 = 结构原型 | OceanCurrentField 已建（风驱方向 + 科里奥利 14° 旋转占位 + 纬度海温）；**大陆折射 → 环流圈/东岸暖西岸冷 = S6.1**，依赖海岸方向场，完成前 current 层海温只是纬度基准 |
| D26 | 生产未切换 | V2 噪声海陆（NoiseContinentGrid）尚未接入生产（TalosLandMask/WorldgenAPI 仍走 TectonicWorld），接入 = X1 阶段2；当前新 L1 只服务气团/洋流原型与 /talosmap |
| D29 | 风场 v2 路线（2026-09 定） | 目标形态 = 方案 C（离线松弛收敛 风↔SST↔气压，运行时查表插值），按 B 序建场：M1 ThermalForcing（insolation/Teq 海陆/SST 骨架/大陆度/直减）✅；M2 PressureField（动力剖面高斯带 + 摆动 + 热力项 → 地转+摩擦地表风）✅（纬向剖面/三圈/辐合结构/赤道无风带已验）；M3 气团上游输运 ✅（AdvectedAirField：沿 PressureField 风回溯 7 步，温度/湿度/海洋性弛豫；实测：迎风岸湿舌、内陆连续干区、海洋性不对称渗透）；M4 洋流 v2 ✅（OceanCurrentField 重写：M2 风 + 埃克曼转向 + 岸墙折射(贴岸 4k 全切向/26k 衰减) + SST 沿流输运；局部放大验证 ~90° 沿岸滑行，近岸 23% 采样转向>45°；**环流圈/暖寒舌的整体组织待 M5 离线松弛积分**）；M5 离线松弛 ✅原型（环面 200×100 格嵌套迭代：内层固定流场收敛海温、外层更新气压风；已验证：**西岸暖/东岸冷、北顺南逆副热带环流圈、冷舌涌现**；已知：副高带仍未断成中心（气压增益/外层流场欠弛豫待调，压差残差图可直查），气压海温闭环 RMS≈0.016 量程 2 的 0.8%；求解器已移植入库：**RelaxedClimate**（按种子缓存 + 双线性查询 API：samplePressure / sampleWind / sampleSst / sampleCurrent；
首次求解约 2.8s/种子，命中缓存后查询为 ~100ns 级 —— M5+M6 主体完成）。
调参记录：海温-气压增益 1.4 → 2.2、外层风欠弛豫 0.5（抑制追逐）；残差气压图（p-纬均）呈现交替正负"行动中心"；闭环 RMS≈0.016（量程 2 的 0.8%）。
**收口接线（2026-09）**：RelaxedClimate 增加空气场（tAir/q/海洋性随耦合流场收敛，~5.1s/种子）；GlobalClimate v2 全面切到查表源 + P1b 地形算子
（辐合对流雨 + 迎风抬升雨 − 背风焚风减雨）；ClimatePreheat（WorldEvent.Load → 后台预求解）已注册，首区块不再卡同步求解。
**降水/干湿精调（2026-09 定版）**：辐合标尺按实测分位重定（8e-6→1e-4，原值致饱和洗平）、陆地平衡湿度 0.05+0.20I→0.08+0.30I、
雨量改为"升力驱动"（rain=q·(0.30+1.6·辐合×下沉抑制)+2.0·抬升·q·陆，−0.5·焚风）；定版分带实测：海 赤道 0.64/副高 0.34/中纬 0.48/副极 0.53/极 0.29，
陆 热带 0.39/副高 0.28/中纬 0.42/极 0.18，干带与 30° 副高压位吻合。
  **沿岸伪影修复**：跨岸双线性会拽入陆格 0 值 → 近岸整格色块；solve 收尾加 ghostFillSea（临海 8 邻域陆格用海值填充 sst/流场，2 遍），查询端 sampleCurrent 去陆上空判定。**已知**：副高分段"行动中心"需 M4/M5 洋流-海温反馈才出现（真实副高中心 = 冷洋+季风槽切割），M2 不强拧参数 |
| D27 | 自适应阈值（2026-09） | 固定阈值对低频噪声的种子间均值漂移极敏感（0.607 下陆地占比实测 22%~54% 摆动）。改为按种子标定：首用在该种子主域 4k 网格采样 5000 点取 q67+LIFT/2 作阈值并缓存 → 各种子陆地稳定 ≈33%（D10）。**已知**：大陆块数与均衡度仍随种子变（3~7 块；个别种子出一块 80%+ 泛大陆）；强制 4~6 均衡大洲需布点格+多极点方案（四节），暂缓 |
| D28 | L1b 地形骨架场 OrographyField（2026-09 定稿） | 给新海陆加"哪里是山"的指示场，**双轨输出**：A. orography 级连续场（气候/气团/风消费）：`elevation01`（残差 q93 归一=内陆海拔，供直减/雨影/高原热源判据）、`relief01`（λ20k 偏差脊线 1-dev/q95 per-seed 归一）、`beltMask01`；B. 离散 `kind`（LOWLAND/HILL/PLATEAU/MOUNTAIN/PEAK，**保留供下游地形/宏群系/山生成层使用**），占比按种子 dev 分位标定保证（峰=dev 最小 3.5%、山+峰=前 30%、高原=非山地海拔顶 17%→低地+丘陵≈53%；三~四种子实测 低+丘 51~52/高原 17/山 28~29/峰 ~1）。**不从板块假设**：全从 NoiseContinentGrid 自身场派生（山永远在陆上、零新随机；脊线与海拔场独立 → 无等距山环/高原同心圈）。已知教训：① 不能用固定阈值切 ridged（窄动态范围→饱和或 0 并列、分位塌陷）② 分位前必须排序 ③ 山链判定不得掺"离岸距离"等高程相关门。未来 PlateField 只替换 ridgeDev 来源，下游 API 不变 |
| D30 | X1 阶段2 · 块级高度映射（2026-09 定稿，T1.2） | 生产 seaLevel=64（ChunkProviderTalos2.getWaterLevel 覆写）/worldHeight=256。4 种子探针：陆地恒 0.330、landQ93 0.30~0.50、seaQ93 0.30~0.37、陆 r q50≈0.16 max≈1.4~1.7×rQ、海 |r| max≈1.3×seaQ、elev 陆 q90≈0.82；单点成本 oroSample≈210ns / coastDist≈346ns。**陆高** = 64+1+36·elev01 + kind 抬升（HILL 8r / PLATEAU 9 / MOUNTAIN 10+52r / PEAK 16+56r+18r²）→ 海岸 65 / 内陆 ~101 / 山脊 150+ / 峰 ~200+；**雪线** = 赤道 158 → 极地 96（bandD 线性插值，V2TerrainGen）。**海床深** = 2 + 32·smoothstep(0.30,1.20,|r|/seaQ93) + 深盆附加（>1.25× 后 ≤+9）→ 岸 2 / 典型 ~30 / 最深 ~43；浅沙底 ≤6、砂砾 ≤16、更深直接岩性变体。**沙滩带**：陆侧 -10..0 且 elev<0.10 铺沙；V2 轨不再用旧 SeafloorShaper/海岸塑形子链。**medNoise 升级**：2→4 级 + 二次域扭曲（0.5 等值线闭环 → 渗透蜿蜒带，出图去"迷宫项链"；分档按每种子分位自适应，占比目标不变）。待游戏内观感精调（山体尺度/雪斑连续性/岸壁） |
| D31 | DLA 山去留（2026-09 拍板，后修订为 D31v2） | **原案**：新轨山 = OrographyField 直接抬升（DLA 不移植）——游戏实测后**废弃**（地形平滑无细节）。**D31v2（2026-09 修订）**：山 = DLA 重挂 V2 类型场。机械全复用（MountainWorldState 洪水发现/山带构建/DLA 生长/beltAt 查询原样），唯一缝 = MountainWorldState.computeStyleTier → TalosTectonicStyles.tierAt：V2 档改由类型场判 tier（山带存在域 = 大区包络 env(λ60k,salt0x517CC1)≥0.40 ∩ kind∈{MOUNTAIN,PEAK}；tier3=PEAK∧belt≥0.72∧elev≥0.30 / tier2=belt≥0.55∧elev≥0.30 / tier1=belt≥0.45∧elev≥0.18；预算 ≈ 9%/5%/1% 陆地）。包络是必须项：dev 走廊全球连通，不包络则整大陆成一条巨型山带。海洋海脊/海沟维持后续单独计划 |
| D32 | 块高与类型彻底分家（2026-09 拍板，X1 阶段2 修订） | L1/L1b 只输出类型（isLand/kind/belt/coastDist）；**elevation01 保留为类型判据 + 气候慢场**（直减只冻真山地、降水迎风坡/焚风），**不再参与块级定高**。块级高度 = V2TerrainGen：类型连续权重（山带走廊×高海拔门 m=smoothstep(0.22,0.60,elev01) 才进山地档，低海拔走廊并入丘陵档——走廊覆盖陆地 5~6 成，不设门则中纬大面积假雪山）× 五档档案（LOW 68-94 / HILL 76-112 / PLATEAU 96-122(台地化0.62) / MOUNTAIN 100-152 / PEAK 136-214，频率全部共享：低1/3000 中1/1100 高1/260）→ 单次 TerrainBaseHeight.computeBaseHeightCore 三层分解（机械复用旧层 TerrainNoise/TerrainBaseHeight，未发明新噪声）。海洋 = 残差深度带（D30 公式）+ 中频海床波纹 ±3。雪线 赤道172 → 极地116（纬度线性）。DLA 抬升在 Provider 叠加于基高之上（MountainTerrainModifier lerp 到脊线/谷底）。实测（出图）：中纬 6000 格窗数百格起伏可见、无纯平死区；全图雪区回落 10-15%、碎裂雪顶；待游戏内目检 + DLA 带观感精调 |
| D33 | L1c 峰核场 SummitField（2026-09 定稿，S1~S4 全做） | 问题：kind=PEAK 是「离脊线近」的标签而非高度排序，DLA 峰顶由其自身锚点生长决定 → Alpine 出现在山地上但不是最高点。**解法（反转因果）**：先指定峰位，再由高度链保证其最高。算法：512 格网格上峰势 P=clamp01(0.45·elev01+0.35·relief01+0.20·belt01)，带 kind 门（低地 P=0）；P 为 8 邻域严格局部极大且 ≥0.42 的格即峰核（并列用打包 key 打破）；每格惰性缓存 + CHM（无限世界可用、无预构建）。**目标高度 T = 支配半径 1024 格圆盘内基底最大值 + boost(14~42)**（DLA 无关、确定性）。高度链：h = 基高 → DLA 抬升（峰顶被 T 与 base+55 双重封顶）→ 峰核收敛 h=min(h,T)+f·(T−min(h,T))，锥半径 220/平台 45、锥腰微起伏 4f(1−f)·±3 格（雪线边界破碎）；盘内 H ≤ T 且中心 = T。群系：Alpine = 峰核影响 ≥0.55 且 T ≥ 该纬度雪线（赤道低峰保持 Mountains）。**数值验收**：438 个峰核中心全部为 ±512 邻域最高点（violations=0）；出图 run/talos_maps/v2_summit_*.png。**已知**：中纬（雪线 ~134-155）峰核 T 多在 130-165，Alpine 主要出现在高纬与最高山体；若想中纬也有雪峰，旋钮 = BOOST_MIN/MAX 上调 或 雪线曲线上移。开关 talos.v2.summitEnabled |
| D34 | V2 山层替换 DLA（2026-09 定稿，过程驱动） | **背景**：噪声/骨架类山形都不自然（波纹/回环/方正/涂鸦）；真实山脊是**侵蚀/水系过程**的产物。**方案**：`MountainLayerV2`（chunk/world/）—— 400k×200k 环面域、**250m 网格（1600×800）**，按种子离线求解：5 条山带（中心尽量内陆、互距≥90k、走向 45~100k/半宽 8~15k 随机）→ 抬升场（山带包络 + 主脊轴蜿蜒 + 沿脊离散峰列 + 多尺度噪声）→ **Priority-Flood+ε 填洼**（fbm 噪声会造出大量小洼地，不填则汇水面积碎片化：实测 accMax 911 → 12.3 万）→ **D8 汇水面积** → **分档静态下切** `cut = carve·log(1+A/aRef)/log(1+aFull/aRef)·min(1, env/envFloor)`，再受 `0.92·uplift` 约束（山脊/峰顶 A≈1 不切 → 峰高 = 抬升场设计值；谷地按汇水量分档切到 carve 预算，谷底不穿底盘）→ 14 轮热力平滑圆化坡面 → 输出 `uplift` / `auth` / `slope01` 三张 LUT（按种子缓存，实测 **0.6s/种子**，与 RelaxedClimate 同款架构）。**已废弃的旧实现**：180 轮迭代 stream-power 侵蚀——它把坡面削成针状尖峰（实测峰形 350→127 一格 220 格落差），且下切量对抬升幅度几乎不敏感（底盘 110、峰顶 221 与参数无关），故改为静态分档下切。**仲裁（D34 核心）**：基础地形分解 `plain`（瘦身档 ≤122）+ `mtnComp = base − plain`，与山层按权威权重合成 `h = plain + (1−w)·mtnComp + w·uplift`，w=1 山层全权、w=0 基础地形原样，中间 9k 平滑过渡（剖面验证无台阶）；块级细节由 `V2TerrainGen.mountainDetail` 补（λ1.1k/420，振幅随山体强度）；接近上限用 soft-min 软封顶（H=252,k=6）。**接入**：ChunkProviderTalos2 V2 轨改走此链；`SummitField` 已删除；DLA 在 V2 轨不再调用（旧轨保留）；ClimatePreheat 后台预求解。**群系**：海→OCEAN/SHELF；mountainish = w≥0.35 或 mtnComp≥25 或 kind∈{MOUNTAIN,PEAK}；Alpine = mountainish 且 (w≥0.25 或 mtnComp≥30) 且 合成高度 ≥ 该纬度雪线；其余 MOUNTAINS/PLATEAU/PLAINS。开关 talos.v2.mountainV2Enabled。**调参入口**：`MountainLayerV2.Tune`（探针改写后 `clearCache()` 重解） |
| D35 | 峰形定稿（2026-09，"底盘压低 + 峰突出"，参数后被 D36 取代） | **定稿参数**（`MountainLayerV2.Tune`）：山带包络 `amp 4~10`、主脊 `axialAmp 60~85`、轴半宽 `0.10×halfW`、沿脊离散峰列（周期 ~8k cos² 尖峰）、`floorDrop 18`、下切 `carve 50`/`aGateLo 100`/`aGateHi 4000`/`carveMaxFrac 0.6`、峰化曲线 γ=1.35、平滑 2 轮；基础山地档案带限下调（MOUNTAIN 96/128→78/104、PEAK 118/190→94/124）。**实测（种子 1022228679，山层核心 auth≥0.6）**：p05=101 / p50=137 / p95=221 / max=252，峰突出度 ~115。渲染图 `run/talos_maps/mtn18b_final_peak64k.png`、`mtn18b_final_overview.png` |
| D36 | "中间高的那块是平的"——Alpine 起伏诊断与修复（2026-09） | **诊断**（250m 网格全域扫描；指标 σ(R) = std of h−boxMean(h,R)，即去掉整体坡度后的局部起伏）：① **Alpine 群系 61% 的面积不由山层生成**，而是基础地形的 MOUNTAIN/PEAK 档（实测 σ(1km)=9 格）；② 山层自身的噪声八度只有 4~45km 波长（振幅 3~8 格），1~4km 尺度上几乎没有结构（σ(1km)=5.9）；③ 下切后的 **14 轮热力平滑**把 0.5~1km 成分削掉 93~99%（实测 R500 19→4、R1000 21→7、R2000 24→12）；④ 下切量被 `cut ≤ 0.92·v` 钳位 → 退化为"整场 ×0.4 缩放"，再被峰化曲线归一化抵消，等于没刻谷。**修复**：① 新增共享中尺度脊线纹理 `V2TerrainGen.mountainTexture`（λ≈3k/1.2k/480m 三档 ridged；山层与基础山地用**同一个场**，脊线对齐），山层 `midAmp 450 / texPow 2.5`、基础山地 `mtnTexAmp 200`；② 平滑 14 → **2** 轮；③ 下切闸门改为汇水面积 100~4000 格 smoothstep（只有真河道才切），`carve 50`、钳位 0.6；④ 基础山地带限下调（见 D35），把高度预算让给纹理。**实测（种子 1022228679）**：山层核心 σ(1km) 10→**27**、高度 p05~p95 82~125 → **101~221**；Alpine 面积 3.7% → **12.2%**、σ(1km) 20→**27**；全域陆地 p95 125→**194**。**硬约束**：世界高度 256——继续加振幅只会让 240+ 区域被软封顶压平（贴顶面积 0.5%→4.7%）。**遗留**：非山层的基础山地呈"疙瘩状"而非连贯山脊（视觉复核确认）；要真正的山脊网络需要方向性构造或更多山带 |
| D37 | 世界高度来源审计 + 非山地群系下压（2026-09） | **审计**（种子 1022228679，250m 全域；脚本 `build/eoh_probe/mtn/P25`）：高度链 = ① L1 海陆（陆地 32.9%、海洋 67.1%，海床 64−2..34）→ ② L1b 类型场 kind（**分位数定义**：KIND_MTN_TOP=0.30 → 恒 32.4% 陆地标 MOUNTAIN、2.0% PEAK、16.3% 高原，与真实高度无关）→ ③ 五档档案权重（陆地均值 LOW .234 / HILL .493 / PLATEAU .050 / MOUNTAIN .133 / PEAK .091）→ ④ `landBaseHeight` = bandShape(低频+大陆骨架)→[minH,maxH] + 中频起伏(±min(18, midAmp·0.9)) + 高频细节(±4) + **中尺度纹理**(0~mtnTexAmp·(wMtn+wPeak)·tex²) → ⑤ `landPlainHeight` 瘦身版（MOUNTAIN/PEAK 并入 HILL）= 世界地板 `plain` → ⑥ 山层 uplift/auth → ⑦ 仲裁 `h=plain+(1−w)·mtnComp+w·uplift` + `mountainDetail`(±34) + softcap(252)。**审计发现**：a) 41% 陆地被标 MOUNTAINS 但中位仅 110——MOUNTAIN 档案带限（78~104）并不比 HILL（76~112）高，且其 mtnComp 均值仅 18；b) 世界地板 `plain` 均值 86 = 海面+22，低地不够低；c) ALPINE 的 mtnComp 均值 100（中尺度纹理主导，非山层）；d) 群系 `rootHeight/heightVariation`（BiomeGenTalos2*）不参与本 mod 地形（ChunkProvider 全接管），仅供查询群系高度的 mod 用。**下压**：LOW 68/94→**68/82**、HILL 76/112→**72/96**、PLATEAU 96/122→**86/110**、MOUNTAIN 78/104→**92/126**、PEAK 94/124→**106/150**。**结果**（同脚本，p05/p50/p95）：PLAINS 74/86/105→**70/79/94**；PLATEAU 80/106/124→**74/95/113**；MOUNTAINS 80/110/169→**74/105/172**；ALPINE 158/197/249→**162/202/251**；全域陆地 p50 96→**87**。**遗留**：MOUNTAINS 群系中位仍 105（kind 分位数所致）——若要"标签=高度"，需收紧 `V2BiomePicker` 的 mountainish（去掉 kind 条件，或 mtnComp 阈值提到 40+）。另：南北半球相关系数 r=−0.03（无镜像重复，渲染图上的"上下相似"是统计均匀性造成的错觉） |
| D38 | L1c 群系场 V2BiomeSelect（2026-09，打破"群系 ← 高度"循环依赖） | **问题**：V2 轨群系是"高度反推的标签"（`h ≥ 雪线 && mountainish → ALPINE`），导致旧宏包的群系高度参数（heightBias/heightScale）在 V2 轨无法参与定高（循环依赖），且只有 6 个占位群系（另 10 个永不出现）。**方案**：新增 `V2BiomeSelect`（chunk/world，纯计算、**不依赖 Minecraft 类型**）——输入 L0 气候场（温度/湿度/海洋性/气压）+ L1b 类型场（relief01/elevation01/kind/coastDist），输出 16 个群系种类 `Kind` + 高度倾向；`V2BiomePicker` 只做 Kind → BiomeGenBase 映射与打包。**判据顺序**：① 海 → OCEAN/SHELF（必须回查 L1 海岸距离，OroSample 海上 coastDist 恒 0）；② `relief01 ≥ 0.75` 且 `elevation01 ≥ 0.30` → MOUNTAINS，其中 `elevation01 ≥ 0.55` 且 `temp ≤ 0.20 + 0.60·elev` → ALPINE；③ `kind == PLATEAU` → PLATEAU；④ 内陆 ≥ 0.5 且 wet ≥ 0.55 且 elev ≤ 0.30 → BASIN；⑤ 其余按（温度 × 湿度）Whittaker 分类。**坐标**：温度 = 0.70·纬度带 + 0.30·气团温度（气团温度分布偏暖偏斜，实测 p25=−0.06 / p50=0.62，单用会把半个世界判成热带）；湿度 = 0.50·空气湿度 + 0.28·海洋性 + 0.32·(1−动力干燥)。**高度接入**：`TerrainBaseHeight.computeBaseHeightCore(..., biomeBias, biomeScale)`（新增 6 参重载；旧 5 参保留 V1 口径 gain=0.25）——`baseT` 先按 scale 压缩散布（factor = 0.4 + 1.2·scale，0.5 = 中性）再按 bias 位移（gain 1.6）；`V2TerrainGen` 的 base/plain 用同一份调制，保证 `mtnComp` 干净；ChunkProviderTalos2 的地表 profile 也改用选中的群系。**实测（种子 1022228679，500m 全域）**：14 个陆地群系全部出现——MOUNTAINS 12.2% / PLATEAU 15.2% / TEMPERATE_FOREST 11.6% / COOL_FOREST 10.2% / POLAR_DESERT 9.5% / DESERT 7.3% / SAVANNA 6.3% / PLAINS 5.9% / TROPICAL_RAIN 5.6% / ALPINE 4.8% / TEMPERATE_STEPPE·WARM_STEPPE·SUBPOLAR_TUNDRA 2.6~3.1% / BASIN 2.6%；各群系高度 p50：**ALPINE 206 / MOUNTAINS 173** / PLATEAU 136 / PLAINS 84 / BASIN 75 / POLAR_DESERT 71。**开/关群系高度倾向对照**：MOUNTAINS 157→173、ALPINE 191→206、BASIN 81→75、POLAR_DESERT 79→71，全域陆地 p50 87→84（低地整体下压、山地抬升）。群系图 `run/talos_maps/biome_v2select.png`。**遗留**：① 群系边界硬切（无平滑层，视觉复核指出"雪山紧邻沙漠、过渡带缺失"）→ 需 T2.2 群系平滑（连通分量吞并 + 邻域混合，旧 `BiomeRegionLayer` 可参考）；② 可调旋钮：`V2BiomeSelect` 的阈值、`TerrainBaseHeight.BIOME_BIAS_GAIN`（群系高度压制力度） |
| D39 | 群系气候坐标 ClimateCoords + 气候带软分配（2026-09，B1~B3 落地） | **问题**（D38 遗留）：群系只消费 4 个标量，风带/洋流/气团未接入；权重是手调线性组合；单一 argmax 硬切；16 群系写死。**方案**：新增 `ClimateCoords`（L1c 输入层）把 M1–M6 环流解收口成两个有物理含义的坐标 + 内陆度：`tempEff = 0.70·纬度带 + 0.30·气团温度 + Δ气团 − 0.55·elevation01`（海拔直减用**类型量** elevation01，无循环依赖，删掉旧 Alpine 特判）；`moistEff = 0.25 + 0.55·q + 0.25·mar + 0.20·(1−dry) + 0.30·迎风抬升·q − 0.18·焚风·q ± 0.9·海温距平 + Δ气团 − 0.12·内陆度`。三项物理各带独立开关：`ENABLE_ORO`（风×∇elevation01，与 GlobalClimate P1b 同口径）、`ENABLE_SST`（上风 60km 海点海温距平；纬度期望用实测 10 档插值）、`ENABLE_AIRMASS`（cT/mT/cP/mP 签名）。`V2BiomeSelect` 改为**气候带软分配**：5 温度带 × 4 湿度带查表，帐篷隶属度取最多 4 个候选；地形变体（山地/高山/高原/盆地）以连续门控权重一起归一化；**高度倾向按权重插值**（地形连续，无台阶），群系 ID 取 argmax。**实测（种子 1022228679，500m 全域，陆地 32.9%）**：14 个陆地群系占比 PLAINS 12.7 / MOUNTAINS 11.6 / PLATEAU 10.9 / TEMPERATE_FOREST 9.9 / POLAR_DESERT 9.1 / WARM_STEPPE 9.0 / ALPINE 6.4 / SAVANNA 5.4 / COOL_FOREST 5.4 / TROPICAL_RAIN 5.0 / SUBPOLAR_TUNDRA 4.7 / DESERT 4.0 / BASIN 3.3 / TEMPERATE_STEPPE 2.6 %；各群系高度 p50：ALPINE 177 / MOUNTAINS 156 / PLATEAU 123 / 低地 72~82。**物理项 A/B**（湿度坐标 p05 / DESERT / TEMPERATE_FOREST）：全关 0.38 / 0.0% / 18.1% → +气团 0.33 / 1.7% / 11.8% → +风带地形 0.32 / 2.8% / 10.4% → +洋流 0.27 / 4.0% / 9.9%。**出图**：`run/talos_maps/v2_map_biome.png`（群系）、`v2_map_wind.png`（风+风速）、`v2_map_current.png`（洋流+海温）、`v2_map_mixed.png`（混合+海岸线）。**环流层实测（P34）**：风/洋流**纬向带结构正确**（信风 u≈−1.47、西风 +0.70、极地东风 −0.53；洋流同相位），但**经向分量仅 ~5%**（v≈±0.05）、涡度 ~1e-6 → **环流圈/西边界流很弱**（与 D29"已涌现环流圈"的记载不符，属环流层待修项，非群系层问题）。**遗留**：群系平滑（B6/T2.2）未做；Tier-3 特殊变体（沼泽/盐沼）留接口 |
| D40 | 环流层修复：洋流模长 / 西边界急流 / 海温弛豫长度（2026-09） | **诊断纠正**：D39 里"环流圈很弱"的结论是**度量错误**——当时用的是分纬度**有符号均值**，经向分量正负抵消；改用绝对均值后实测 `|v|/|u| = 0.5~0.8`、48~83% 的海格 `|v| > 0.2`，流线追踪图（`v2_map_current_stream.png`）确认存在闭合环流圈。**真问题 1**：`updateFlow` 先把风矢量归一化再旋转 → **洋流模长恒为 1.000**（纯方向场，无强弱之分）。改为旋转**整个矢量**（保留风速量级）。**真问题 2**：无西边界强化。新增示意 WBC：南北向海岸上西边界向极、东边界向赤道，`fz += s·side·BC_GAIN·(1−d/BC_WIDTH)²·|wind|`（BC_WIDTH=100k、BC_GAIN=1.2）；实测离岸<100km 西边界经向 +0.104、东边界 −0.181（方向正确）。**真问题 3（最重要）**：`advectSst` 的 `relaxL = 55_000` 太短（真实海洋 SST 弛豫 30~60 天 ≈ 250~500km）→ 84km 路径上异常只剩 `exp(−84/55)=22%`，**洋流对海温几乎没有影响**。改为 250km、回溯步数 6→14。实测海温距平 p05/p95 由 −0.18/+0.12 → **−0.43/+0.19**（约 4 倍），群系随之变化：DESERT 4.0→**5.5%**、TEMPERATE_FOREST 9.9→10.3%、COOL_FOREST 5.3→6.0%。**真问题 4（收口）**：西边界强化改用**积分形式 Sverdrup** 求解——`∫_west^east curl(τ)dx = [v] − ∂/∂z(∫u dx)`，洋盆主项 = −∂(行平均纬向风)/∂z，按实测切变自归一（峰值 ±2.5），只在离岸 40 格（80km）内按 decay² 生效 → 方向随纬度自动交替（副热带向极、热带向赤道、副极地向赤道，南北半球随风场自动镜像，无需手写半球因子）。另加**沿岸海温参数化**（未解析边界层）：西边界按回流**向极分量**叠加暖舌（`+0.13·s·wbc·decay²`）、东边界按上升流叠加冷舌（`−0.22·decay²`）。**实测（P41，按行内海段距离分类）**：西边界(dW<10格) 距平 **−0.123** vs 东边界 −0.261 → **西−东 = +0.138**（改造前为 −0.024，符号错误）；纬度剖面实测重标定后 `sstAnom` p05/p95 = −0.19/+0.13，群系随之微调（DESERT 4.6%、WARM_STEPPE 9.3%）。 |
| D41 | 群系 LUT + 空间平滑（T2.2，性能优先） | **动机**：① 群系选择原本**逐列**采样气候场（4 次 LUT + 风 + 4 次 elevation01 + SST ≈ **1031 ns/列**，一个区块 256 列 ≈ 264 µs）；② 群系边界是硬切（视觉复核："雪山紧邻沙漠"）。**方案**：新增 `V2BiomeField`（chunk/world）——按种子**离线求解 1km 网格**（400×200 = 8 万格，实测 **81~89 ms**，跑在 ClimatePreheat 后台线程），逐格调用 `V2BiomeSelect.accumulateWeights`（与单点选择同口径）得到 16 通道候选权重 → argmax 得群系种类、加权得高度倾向；再做 2 遍**邻域众数滤波**（5×5，去孤立小斑块）+ **高度倾向邻域平均**（边界处地形连续）。运行时 `sample(x,z,seed)` = 高度倾向双线性 + 种类最近邻，**线程本地复用结果对象 → 零分配**。**实测性能**：逐列群系查询 **1031 ns → 27.1 ns（38×）**，每区块省约 257 µs；求解 81 ms/种子（对比气候场 ~5 s、山层 ~0.75 s，可忽略）。**平滑效果**：12.07% 采样点群系改变（边界/碎斑被吞并）；连通斑块数（4 邻域、1km 网格）PLAINS 15 块 / MOUNTAINS 17 块 / ALPINE 9 块 / PLATEAU 16 块（平均 51~255 格/块），视觉复核确认"边界连贯、碎斑明显减少"。**接入**：`ChunkProviderTalos2.fillLandColumnV2/columnHeight` 改用 LUT（注意 Sample 为线程本地复用对象，须立即读入局部变量）；`WorldChunkManagerTalos2` 走 `V2BiomeField.kind`；`ClimatePreheat` 增加 `V2BiomeField.ensure`。**调参**：`V2BiomeField.CELL`（分辨率）、`BLUR_R`（平滑半径）、`MODE_PASSES`（滤波遍数） |
| D42 | 区块生成性能优化（2026-09，逐项实测） | **热点定位**（探针 P43~P47，2 万~200 万次采样）：① **岩性变体 `CaveMath.rockVariant3D`（3D fBm）58.9 ns/方块 × 每列 ~95 个石头方块 = 5.6 µs/列 → 每区块 1.33 ms**，是最大热点（列高度链仅 0.26 ms）；② 群系逐列气候采样 1031 ns/列；③ `landBaseHeight`+`landPlainHeight` 两次独立算同一套噪声 721 ns/列；④ 噪声实例走 `ConcurrentHashMap<Long,...>`（装箱 + 哈希）。**优化**：① **岩性两级缓存**——区块级 3×3×17 的 fBm 网格（153 次采样）+ **逐列**把 y=0..h 的岩性算进 `colVar[]`，方块查询退化为数组读；`rockVariant3D` 拆成 `rockValue3D` + `rockVariantFromValue` 以便插值；岩性对做成静态常量（原来每方块 `new BlockMetaPair`）。实测 **58.9 → 5.09 ns/方块（12×）**，每区块 **1.33 ms → 0.115 ms**；岩性不一致率 **0.311%**（240 格尺度场按 8/8/16 采样插值，只影响岩块边界 1 格以内）。② 群系改 1km LUT（见 D41）：**1031 → 27 ns/列**。③ `TerrainBaseHeight` 拆出 `Noise`/`sampleNoise`/`fromNoise`，base 与 plain **共享同一份噪声**：721 → **464 ns/列**。④ 噪声实例改 16 槽直接映射缓存（不可变 Slot 安全发布）：~10~15 次查表/列。**合计**：列高度链 1228 → **935 ns/列**（每区块 0.31 → 0.24 ms）；加上岩性，每区块「可控部分」**1.65 ms → 0.35 ms（4.7×）**。**剩余热点**：`OrographyField.sample` 333 ns/列（其中 `coastDistBlocks` 346 ns 仅陆地列调用）、`mountainDetail` ~100 ns/列（高频场，不适合 LUT）。 |
| D43 | 地貌场 LandformField（R1~R3，根治"山区变沙漠"） | **问题**（实测种子 1022228679）：h≥160 的高地形里 **40.6% 不是山群系**（POLAR_DESERT 20.6% / COOL_FOREST 5.2% / TEMPERATE_FOREST 4.2% / DESERT 1.2%…）。根因两条：① **地形层与群系层各自推导"这里是不是山"**——地形用 `beltMask01×elevation01`（+山层 auth），群系却用 `relief01`（λ20k 脊线接近度，是另一回事），实测 `h=176 belt=1.00 elev=0.49` 的山 `relief01=0.68 → 山地权重 0.06`；② **权重竞争不公平**——气候带候选从 2×2 个带各拿一份、总和常 >1，把上限为 1 的地貌变体压过去（`h=134` 的山：山地权重 0.64 vs DESERT 累加 0.82）。后果：群系判成 DESERT → `TalosSurfaceRegistry` 里 DESERT 表层 = sand+砂岩 → **没有雪线/陡坡分支的地方就铺沙子**。**根治**：新增 `LandformField`（1km 离线 LUT，求解 67~81ms）——把五档档案权重公式（低地/丘陵/台地/山地/峰）抽成**唯一权威**，并输出 `mtnAmt = max(山层 auth, mtnComp0/70, mtn+peak 权重)`；**地形层**（`V2TerrainGen.buildProfileFromWeights`/`basePlainFromWeights`）与**群系层**（`V2BiomeSelect` 的 Tier-2）都只消费它；两个维度改成**混合而非竞争**：`final = (1−d)·气候 + 地貌`（d = 地貌置信度），气候候选先归一化到 1；平滑层的众数滤波按 `1+6·conf` **加权投票**，强山体不会被周围气候群系吞掉。**结果**：h≥160 的非山群系 **40.6% → 0.0%**（16165 个采样里剩 2 个边缘格子）；群系高度 p50：MOUNTAINS **159**、ALPINE **184**、PLATEAU 103、平地群系 71~87；陆地占比 MOUNTAINS 22.6% / TEMPERATE_FOREST 12.0% / ALPINE 10.7% / POLAR_DESERT 8.0% / …；列耗时 935 → **931 ns**（地貌 LUT 查表 ~60ns，被省下的逐列权重计算抵消）。**遗留**：R4（地貌×气候二维表 + 干旱山地等组合变体）未做——目前强山体一律 MOUNTAINS/ALPINE，没有"沙漠山地"这种气候变体 |
| D44 | 山地判据改用"实测抬升量"（修 D43 遗留：低地也被标成山群系） | **问题**（实测种子 1022228679）：D43 后 MOUNTAINS **31.3% 低于 110**、ALPINE **16.5% 低于 110 且 38.9% 在雪线以下**——低海拔平地也挂着"山地/高山"群系。**根因三条**：① `mtnAmt` 里含档案权重项 `wMtn+wPeak`，而**档案标签 ≠ 实际高度**（MOUNTAIN 档在低底盘区只有 ~100 高）；② LandformField 求解时算 `mtnComp0` **漏了中尺度纹理**（山地高度主要就来自纹理），于是只能靠档案权重补偿；③ 平滑层用「按置信度加权的众数滤波」——少量高置信邻居的多数票能**把大片低置信区搬成 MOUNTAINS**（实测 (41500,500) 原始 argmax 是 SAVANNA 0.53 却被平滑成 MOUNTAINS）。**修复**：① `mtnAmt` 改为**实测口径**：`rise = (1−auth)·mtnComp0 + auth·uplift`（含纹理），`mtnAmt = rise / MTN_RISE_SCALE`（85），删掉档案权重项；② LandformField 求解改用 `V2TerrainGen.basePlainFromWeights`（含纹理）并额外存 `h0`（中性骨架高度）；③ ALPINE 增加**雪线门控** `ss(snowLineY, snowLineY+30, h0)`（用静态骨架高度，无循环依赖）；④ 平滑层改为对**16 通道权重场做可分离盒滤波**（半径 1、2 遍 ≈ 2km）再取 argmax——边界落在权重交叉处，不再被多数票搬走。**结果**：MOUNTAINS p05 75→**117** / p50 132→**167** / h<110 占 31.3%→**6.1%**；ALPINE p05 79→**148** / p50 161→**203** / 低于雪线 38.9%→**12.2%**；平地群系 p95 全部回落到 92~129。陆地占比 MOUNTAINS 22.9% / TEMPERATE_FOREST 12.8% / COOL_FOREST 8.6% / POLAR_DESERT 8.0% / TROPICAL_RAIN 7.6% / WARM_STEPPE 7.2% / ALPINE 5.6% / …。**可调**：`LandformField.MTN_RISE_SCALE`（山体门槛）、`V2BiomeField.BLUR_R/BLUR_PASSES`（边界裙边） |
| D45 | 系统级代码审查与清理（2026-09） | **审查目标**：找出 D43/D44 那类"两层各自推导同一概念"的隐患。**发现并修复**：① **`WorldChunkManagerTalos2.biomeCache` 无上限**（每个查询过的 (x,z) 一条，长会话下可达数百万条 → 内存泄漏）；改为有界（8192 条，超限整体清空）。② **`RelaxedClimate.CACHE` 无上限**（每个解约 1.3MB，多世界会累积）；加"最多 3 个种子"保护（与 MountainLayerV2/LandformField/V2BiomeField 一致）。③ **死代码**：`V2BiomePicker` 的 `Sel`/`selectAt`/`landBiome`/`seaBiome` 全部无引用 → 删除（只留 `biomeAt`/`biomeOf`）。④ **文档漂移**：`V2TerrainGen` 类注释仍写"DLA 山脉抬升由 Provider 叠加（TalosMountainSystem）"、`snowLineY` 注释写"赤道 158/极地 96"（实际 185/128）→ 修正。**核对通过的不变式**（P55，500m 全域）：陆列判海群系 0 / 海列判陆群系 0；`bias ∈ [0.15,0.72]`、`scale ∈ [0.20,0.60]`（正好是 Kind 表范围）；各群系 `mtnAmt` 均值干净分层——MOUNTAINS 0.69 / ALPINE 0.90 / PLATEAU 0.09 / 其余 ≤0.11；高度 p50 MOUNTAINS 164 / ALPINE 198 / 平地群系 72~88（p95 全部 ≤143）。**已知重复（保留）**：`V2BiomeSelect.Kind` 的 heightBias/heightScale 与 `BiomeGenTalos2*` 的同名字段同值（后者仅 V1 轨使用）；`V2BiomeField.CELL` 与 `LandformField.CELL` 各自独立（都是 1000m） |
| D46 | `/talosBiome` 找不到 Alpine（可达性排查 + 搜索重写） | **现象**：用户用 `/talosBiome alpine 100000` 提示"半径 100000 格内未找到"。**排查**（探针 P56/P57/P58，种子 1022228679）：Alpine 本身存在且占**陆地 6.47%**（1702 个 1km 格），但**只长在 5 条山带上**，因此"离最近 Alpine 的距离"分布极不均匀——43.7% 的陆地 ≤25km、30.5% 在 25~50km、22.3% 在 50~75km、3.2% 在 75~100km、**0.3% 在 100~125km**，全图最大 **110km**；实测锚点 (100000,50000) 最近 Alpine 在 **101.3km** 处——**刚过 100k 半径**，所以命令没错、只是搜索口径（沿 x/z 轴块级粗扫 + 球面/平面距离）既慢又漏。**修复**：V2 轨改为**直接扫群系 LUT**（400×200 个 1km 格中心，8 万次 `V2BiomePicker.biomeAt`，瞬时完成、覆盖全图），距离用**环面折叠** `wrapDistSq`（x 周期 400k、z 周期 200k，避免接缝处选到远点），命中格内再 ±1024/步长 32 精扫；默认半径在 V2 轨直接取全球（`MAX_RADIUS=200000`），旧轨仍 30000。**验证**（P58，复刻命令算法）：4 个起点全部命中且命中点复查为 ALPINE，(0,0)→48.4km、(100000,50000)→101.3km、(250000,60000)→0km、(330000,160000)→40.3km。**产物**：距离场图 `run/talos_maps/v2_alpine_dist.png`。 |

### 依赖图（单向，权威因果链：海陆 → 气团 → 风 → 洋流）
```
① 太阳辐射不均匀加热 → 赤道-极地温差（基础）
② 海陆分布(L1)           ← 决定下垫面（大陆/海洋热容量、粗糙度差异）★ 一切前提
③ 气团(L2)               ← 依附海陆（源地上垫面）：海→暖湿mT/冷湿mP，陆→干cP/cT
④ 风场(L3)               ← 三圈环流基底 + 气团间气压差 + 海陆热力差异(季风)
⑤ 洋流(L4)               ← 风驱动 × 海陆阻挡折射 → 环流圈/东西岸对流
洞穴/洞厅/湖心岛 = 独立子系统（4096 超级格，只依赖 isLand 门控）
```

---

## 二、L0 纬度循环重定义（方案 A，改 ClimateLatitudes）

### 新几何

| 项 | 现状 | 新方案 |
|---|---|---|
| 周期长度 | LAT_CYCLE = 100k | **200k**（-100k ~ +100k） |
| 热带中线 | 0, ±100k, ±200k | **0, ±200k, ±400k** |
| 寒带位置 | ±50k, ±150k | **±100k, ±300k** |
| 周期边界温度 | 热带（热接热） | **寒带（冷接冷）** |
| 5 档阈值 | 8k/16k/32k/42k | **16k/32k/64k/84k** |
| MAX_D | 50k | **100k** |

带序（-100k → +100k）：寒 → 亚寒 → 温 → 亚热 → 热(0) → 亚热 → 温 → 亚寒 → 寒

### 消费方评估（已完成，全部在 climate_layer 包内）
- MacroSites / MacroSitesSeparated：getBelt(iz) + belt.ordinal() 参与站点哈希 → 带标签平移，无接口破坏
- MacroClimateConfig：候选表内容不变
- 跨层（continent/terrain/river/mountain）无引用
→ 只需改 ClimateLatitudes + 重跑种子。

---

## 二b. 世界环面几何（洋流的地基）

**把 X 方向也接成周期** → 世界从"无限平面"变成**双周期环面**：

```
X 周期 = 400k（-200k ~ +200k 首尾相接）
Z 纬度循环 = 200k（-100k ~ +100k 首尾相接，寒带接寒带）
→ 任意方向走出去都会从对面回来 → 洋流可以闭合转圈
```

- **重要区分**：此周期只作用于 **L0 理论场**（洋流/风/气压的"无大陆理想行星"）；
  大陆 L1 仍然无限延伸、不规则、不随 X 周期。洋流查询 = L0 理论场 + L1 大陆折射修正。
- X 周期选 400k（比 Z 的 200k 大）→ 环流圈舒缓、图案重复感低。
- **热带仍在 Z=0**（几何平移确认：0 点为热带中线，±100k 寒带，出生区朝向不受颠覆）。

### 环面语义（墙纸平铺，非独立区域）
- 世界 = 一个 400k(X) × 200k(Z) 周期域在无限平面上**无缝平铺**；不存在"区域之间缝合"——
  任何世界坐标先折叠到主域 [0,400k)×[0,200k) 再采样，结果即该点值。
- L0 场函数自带周期性：洋流/风/气压跨边界天然连续（场(x,z)=场(x+400k,z)=场(x,z+200k)）。
- **远洋理论场周期性重复已接受**（每 400k×200k 图案重现）；靠 400k 足够远 + 大陆不周期掩盖。
- **大陆不卷绕（已定）**：大陆在无限平面布点，跨越纸缝（x=400k）自然截断，不做卷绕镜像。

---

## 二c. 最终架构：气候系统统一层（海陆→气团→风→洋流）

**打包成一层统一 API 对外**：内部 L1~L4 细节不外泄，只暴露顶层接口返回完整 ClimateSample。

```
climate_layer / circulation_layer（统一对外）
  GlobalClimate            ← 对外唯一 API（返回 ClimateSample）
    ├─ L1 LandOceanField  海陆分布（isLand + 海岸距离 + 大洲归属）S3
    ├─ L2 AirMassField    气团（依附海陆：海→mT/mP，陆→cT/cP）S4
    ├─ L3 WindField       风场（三圈基底 + 气团气压差 + 季风）S5
    └─ L4 OceanCurrentField 洋流（风 × 海陆折射 → 环流圈/岸流）S6
```

气团种类（NOAA 标准 4 类，依附海陆）：
| 气团 | 缩写 | 源地上垫面 | 常驻系统 |
|---|---|---|---|
| 极地大陆气团 | cP | 高纬陆地/冰盖 | 极地高压(在陆地) |
| 极地海洋气团 | mP | 高纬海洋 | 副极地低压(冰岛/阿留申) |
| 热带海洋气团 | mT | 低纬海洋 | 副热带高压(在海洋) |
| 热带大陆气团 | cT | 低纬陆地 | 副热带陆上热低压 |

---

## 二d. 气团/风场定稿（2026-09 修订，D21/D22）

早期草案（已废弃）：气团种子网格 G=250k、体量半径 30k、种子源地约束 → 气团气压梯度地转风。
该实现（AirMassSystem）存在环面折叠与格距不自洽的问题，且与 S5 的三圈基底模型重复，已删除。

定稿模型：
- **气团（L2，S4）** = AirMassField 简单分类：isLand × bandD → cP/mP/mT/cT 四类 + 连续温度/湿度；
  block 查询 O(1)（一次海陆采样 + 一次纬度查表）
- **风场（L3，S5）** = 三圈环流纬向基底（权重 1.0）+ 15° 斜向 + 半固定气压系统弱扰动
  （切向 0.18 / 径向 0.08；系统=真实大气行动中心：ITCZ/副热带高压/副极地低压/极地高压）；
  系统距离按 X=400k / Z=200k 最小镜像回卷，接缝两侧连续
- **洋流（L4，S6）** = 结构原型：表层流 = 驱动风旋转 14°（科里奥利方向）+ 纬度海温；
  大陆折射环流（东岸暖/西岸冷）待 S6.1（D25）
- 全链路统一出口：GlobalClimate.sample → ClimateSample（D24）；block 查询 = 固定次数求和 → O(1)

---

## 三、L0 环流层（独立包 chunk/circulation_layer/）

### 包结构（2026-09）
```
chunk/circulation_layer/
  GlobalCirculation.java    // 纬度带 + 三圈基底风 + 半固定气压系统（干湿/雨）；环面最小镜像距离
  CirculationSample.java    // L0 输出 DTO（bandD/windX,Z/gyreWarmth占位/pressureDry/rainfallBase/system）
  PressureSystemType.java   // ITCZ/副热带高压/副极地低压/极地高压（标签+旋向+基准干湿）
  AirMassType.java          // cP/mP/mT/cT（标签+海陆+冷热）——位于 continent_layer，与海陆同层
  AirMassField.java         // L2 气团采样（continent_layer，消费 NoiseContinentGrid）
  OceanCurrentField.java    // L4 洋流结构原型（风驱+科里奥利旋转+纬度海温；折射=S6.1）
  GlobalClimate.java        // ★ 统一门面（二c：对外唯一 API）
  ClimateSample.java        // 门面输出 DTO（L1+L0+L2+L4 一次编排）
```
（OceanGyreField 已回退删除；AirMassSystem 种子地转风已废弃删除，见 D21）

### 风场塑造（S2，已按 3 圈环流重写）
按权威机制：太阳辐射不均匀加热 → 赤道-极地温差 → 气压差 → 风，再被科里奥利扭向 → 三圈环流。
- Hadley 圈(信风)：东西向 ← + 15° 斜向朝赤道；带状干湿：赤道低压湿
- Ferrel 圈(西风)：东西向 → + 15° 斜向朝极地；副热带高压干
- Polar 圈(极地东风)：东西向 ← + 15° 斜向朝副极地；副极地低压湿、极地高压干
- 南北镜像由科里奥利；大陆影响（季风/海陆热力）留 S3。

### 洋流生成方式（S6 结构原型已建；大陆折射 S6.1 再做）
1. **风驱纬向基底**：信风带往西、西风带往东（GlobalCirculation.windDir 已提供）；
2. **大陆折射**：大陆生成后，洋流绕大陆 → 形成环流圈 + 东岸暖/西岸冷的边界流（S3 大陆完成后实现）；
3. **旋向**：科里奥利（北顺南逆）；
4. **冷暖随流向**：暖流红箭头/寒流蓝箭头（不用纬度底色）。

### 输出场
bandD / windDirX/Z / gyreWarmth / pressureDry / rainfallBase

- pressureDry **显式输出沙漠带信号**（副热带高压带 → 干燥），供 L1 布点 / L5 群系消费（D19）。
- 洋流温度修正：**只作用于沿岸一段**（近岸数百~数千格），远洋按纬度基准（D16 折射前理想环流仍全洋盆可见）。

### 调试诉求
漩涡中心是**显式实体** → 出图时可叠加画出"这里有个暖漩涡"，便于调参。

---

## 四、L1 海陆/板块

- 保留布点格/主次大陆/分离检查/API 字段（下游零改动）
- 替换 Supercontinent.radiusAtAngle 星形 → **域扭曲 + 多极点（每大陆 2~4 极）**
- 弱耦合 L0：L0 给板块偏好场，L1 加权布点
- 大陆密度目标 30~35%（出图后调参数）
- **大洲数量**：400k×200k 环面域内 **4~6 个大洲**（布点格边长 ~80k~100k，S3 验证）
- **岛链**：显式在汇聚边界/大陆边缘撒群岛（岛弧），需要时允许大陆桥接触
- **海绝对连通（D17）**：大陆几何不得围出封闭死海；若多极+扭曲意外围出，中心低洼按陆地处理由水系成湖
- 布点格密度：待定（400k×200k 周期域内目标大陆数量决定）
- **注意**：大陆不随 X 周期；跨 X=±200k 边界不卷绕（大陆自然截断，L0 周期只保证洋流自洽）

---

## 五、L2 山脉

- 山带**只在陆地生成**（新约束）
- 海洋：海脊（洋中脊）/海沟（俯冲带）—— 后续单独做
- 大陆内部：裂谷 —— 后续单独做
- 山带贴弧形汇聚边界（视观感决定）

---

## 六、L3 水系（贯穿巨河）

- 每大陆**至少 1 条贯穿型巨河**（源头在内陆高地/分水岭 → 弯曲 → 入海/大湖），跨越大半个大陆，最好多条
- 完整水系：主干 + 多级支流（树状不交叉）+ 各类湖泊（构造/冰川/牛轭/湿地）
- 河流有头有尾，不凭空消失
- **宽度渐变**：Strahler 序越高越宽——源头河谷 10~20 格半径，入海口 200~400 格（越往下越大）
- 替换模板链 → WatershedBuilder（骨架高程场 → 流域并查集 → 主干逆流回溯 → 支流递归）
- 保留下游 RiverNetwork/塑形/平滑
- 密度由 L0 rainfallBase 决定

---

## 七、L4 盆地/湖/峡谷

- 落地 basinMask01（闭合低洼 → 干盆地/盐沼/湖）
- 峡谷 = 主干河陡坡下切掩码

---

## 八、L5 气候群系层（完全重做，S5）

- 群系选型以 **L0 气候向量**为标准（温度=纬度带+洋流修正；湿度=气压带+风带+距海远近）
- 引入**海洋群系**：暖洋流（暖水群系）/ 寒洋流（冷水群系）/ 温带洋 / 冰洋（浮冰）→ 便于后续海洋装饰
- 保留 MacroSites 站点骨架（缓存结构），重写"选宏包依据"
- BiomeOverrideProvider / smoothing 视需要保留或重做

---

## 九、性能预算

| 层 | 一次性 | 运行期单点 | 缓存 |
|---|---|---|---|
| L0 环流 | 谱系数构建 | ≈0 | tile |
| L1 海陆 | 极点/瓣表 per 大陆 | 2~4 warp | tile+大陆 |
| L2 山脉 | 山带构建 | ≈0 | 大陆 |
| L3 水系 | 流域并查集 per 大陆(ms) | ≈0（空间索引） | 大陆 |
| L4 地貌 | 局部表 | ≈0 | 局部 |
| L5 气候 | — | +1 查表 | tile |

---

## 十、测试方案（方案一：游戏内地图导出 PNG）

新命令 `/talosmap <layer> <cx> <cz> <radius> [stride]`：
- land / gyre / wind / pressure / biome / river / mountain
- 降采样采样 → BufferedImage → PNG（项目已用过 ImageIO）
- 输出到 run/talos_maps/（或玩家可访问目录）
- 上限 1024×1024，超限自动降 stride
- 默认配色：海深蓝渐变、陆绿→棕→白(高度)、暖流红橙/寒流蓝青、河深蓝、山褐白

---

## 十b. ⚠ 待定项（不阻塞 S1，但需在对应阶段前定）

1. **布点格密度**：**已定 4~6 个大洲** per 400k×200k 环面域。现布点格边长 40k → 需加大到 ~80k~100k（400k 域内 4×2~5×2 个候选格，主/次大陆筛选后约 4~6 个实体大洲）。精确边长 S3 出图验证后定
2. **海洋"温度修正"沿岸作用宽度** 具体数值（数百~数千格量级）——S2 出图后按观感调
3. **巨河入海口 200~400 格宽度** 的河谷雕刻上限成本核算——S4 前验证可行
4. **远洋海洋群系是否按洋流染色**（暖洋 vs 寒洋全域）——S4/S5 前定（洋流随大陆折射后才有意义）

---

## 十c. 洋流权威依据（决定实现顺序）

依据标准海洋学（WHOI 伍兹霍尔 / National Geographic / LibreTexts 物理海洋学《Surface Gyres》）：

- **表层洋流由风驱动**：信风(低纬)推水向西、西风(中纬)推水向东；
- **环流圈(Gyre) = 风 + 大陆弯曲**：没有大陆，海洋只有均匀纬向流；大陆阻挡才把水挤成闭合环流；
- **科里奥利力决定旋向**：北半环流顺时针、南半逆时针；
- **东西岸冷暖不对称(boundary currents)**：大洋西边界(大陆东岸)窄而强暖流；东边界(大陆西岸)宽而弱寒流。

**结论**：风场是发动机、大陆是模具。洋流必须排在大陆之后。
风场按三圈环流塑造（已实现）：信风/西风/极地东风各带 15° 斜向（朝赤道/朝极地/朝副极地）。

---

## 十一、实施顺序（最终权威版：海陆 → 气团 → 风 → 洋流）

0. **X1 · 海陆完整重构** —— 河流/山带/群系最终全按噪声海陆工作
   - 阶段1：NoiseContinentGrid（噪声大陆场：height/isLand/coastDistBlocks 块级海岸距离 +
     landResidual/medNoise 结构接口）✅（形态搜索标定：λ=80k×3层+中频0.10；**自适应阈值** D27 →
     各种子陆地 ≈33%；大陆块数随种子 3~7 块）
   - 阶段1b：OrographyField 地形骨架场（D28）✅ 原型——orography 级连续场（elevation01/relief01）出图验证；
     生产山带/群系消费待 X1 阶段2~3
   - 阶段2：V2 轨接入生产 —— ✅（2026-09 T0.2/T1.1~T1.3，integration-worklist.md）：开关
     V2TerrainConfigSection.terrainV2Enabled 分轨；新 L1/L1b → V2TerrainGen（D30）→
     ChunkProviderTalos2.generateTerrainV2 直接出方块；WorldgenAPI/TalosLandMask 本身未换源
     （旧轨保留对照）。图：run/talos_maps/v2_terrain_*_s12345.png。接续：T2 群系 v1 →
     T3 水/洞穴/装饰接缝 → T4 对照收口
   - 阶段3：山带/群系 superId 依赖改接（未开始，随阶段2）
   - 阶段4：河流 WatershedBuilder 自主生成（替代 RVR2 模板链，未开始）
1. **S1 · 改 ClimateLatitudes 200k 周期**（温度边界，✅ 已合入）
2. **S2 · 纬度带/三圈环流基底**（GlobalCirculation 大尺度风带，✅）
3. **S3 · 海陆分布**—— 高度场 + 鞍部抬升合并；isLand/coastDistBlocks ✅（V2 原型层，未接生产）
4. **S4 · 气团层**（依附海陆：海→mT/mP，陆→cT/cP；类型带标签）✅ 定稿（D21）
   - AirMassType（cP/mP/mT/cT 4 类） + AirMassField.sample（isLand×bandD → type+温度+湿度）
5. **S5 · 风场完整**（三圈环流基底主导稳定 + 半固定气压系统弱扰动 + 15°斜向；含环面最小镜像回卷修正）✅ 定稿（D22）
6. **S6 · 洋流层** ⚠ 结构原型（D25）——风驱方向 + 科里奥利 14° 旋转占位 + 纬度海温；
   **大陆折射 → 环流圈 / 东岸暖西岸冷 = S6.1**（依赖海岸方向场，未开始）
   - OceanCurrentField.sample：flowX/flowZ/temperature/speed（S6.1 前 temperature 无折射色差）
7. **S7 · 水系（WatershedBuilder 巨河）+ L4 地貌 + L5 群系重做**
   （贯穿巨河 + 盆地湖峡谷 + 以气候为标准的群系/海洋群系——未开始）

**当前统一验证手段**：GlobalClimate.sample（D24）→ /talosmap 出图：land（新）/landlegacy（旧，对照）/coast/
airmass/current/wind/pressure/rain/band/gyre。S4~S6 各层均已接入该单入口；生产消费方（L5 群系）在 S7 接入。
